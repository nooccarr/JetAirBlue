export const coloredButton = (text, classname) => (
  <button className={`colored-button ${classname}`}>{text}</button>
);

export const clearButton = (text, classname) => (
  <button className={`clear-button ${classname}`}>{text}</button>
);